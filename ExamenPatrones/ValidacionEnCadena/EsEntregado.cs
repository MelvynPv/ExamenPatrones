using ExamenPatrones.Lectores;
using ExamenPatrones.ValidacionEnCadena.Enumeradores;
using System;

namespace ExamenPatrones.ValidacionEnCadena
{
    public class EsEntregado : Verificacion
    {
        //new public TipoMensaje? Verificar(PeticionPedido peticionPedido, DateTime fechaActual)
        //{
        //    bool fechaAnterior = peticionPedido.FechaPedido.CompareTo(fechaActual) < 0;
        //    TimeSpan tiempoTranscurrido = peticionPedido.FechaPedido.Subtract(fechaActual);

        //    if (fechaAnterior)//Ya se entrego
        //    {

        //    }
        //}
    }
}
